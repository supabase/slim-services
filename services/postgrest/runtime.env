# Low-footprint local-dev defaults, baked as image ENV (overridable at runtime).
# Each pooled connection holds a server-side postgres backend (~5-15 MiB inside
# postgres). Upstream default is 10; 2 is plenty for a single-developer stack.
PGRST_DB_POOL=2
